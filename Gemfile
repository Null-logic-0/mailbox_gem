source "https://rubygems.org"

# Specify your gem's dependencies in mailbox_gem.gemspec.
gemspec

gem "puma"

gem "sqlite3"

gem "propshaft"

# Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
gem "rubocop-rails-omakase", require: false

# Generates gemfiles/*.gemfile so CI can run the suite against each
# supported Rails version instead of only whatever this Gemfile resolves -
# see Appraisals.
gem "appraisal", require: false

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
